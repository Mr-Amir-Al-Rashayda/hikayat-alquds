<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      The splash theme. Compose owns everything after this, so the only job
      here is to paint the Jerusalem-stone background before the first frame -
      which is what stops the white flash the web app had to fix on mobile.
    -->
    <style name="Theme.Hikayat.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/brand_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.Hikayat</item>
    </style>

    <style name="Theme.Hikayat" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@color/brand_background</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>
