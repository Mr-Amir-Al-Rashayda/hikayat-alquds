# Build output. `build/` rather than `/build`, so it matches app/build too.
build/
.gradle/
.kotlin/
.cxx/
/captures
.externalNativeBuild

# Machine-specific; every checkout writes its own.
local.properties

# Signing material, never committed.
keystore.properties
*.keystore
*.jks

# IDE
*.iml
/.idea/
.DS_Store

# Generated by the asset export; regenerated from the reviewed sources.
/tools/.export.mjs
/tools/node_modules/
